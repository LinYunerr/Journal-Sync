# Runtime settings may contain platform credentials.
data.json

# Local dependencies and tool output.
node_modules/
npm-debug.log*
.DS_Store

# Local release archives; release contents remain tracked separately.
*.zip
